const add = (first, second) => {

    return first + second;
}

const multiply = (first, second) => {


    return first * second;
}
// export default add;
// export with shorthand tricks

export { add, multiply }